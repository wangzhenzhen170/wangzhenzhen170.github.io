
(function(graph){

	'use strict';

	graph.app.controller('ModalCiPerfCtrl', ['$scope', '$uibModalInstance', 'result', function($scope, $uibModalInstance, result){

		$scope.$modal = $uibModalInstance;

		$scope.heads = result.head;
		$scope.rows = result.row;

	}]);


})(nameSpace.reg('graph'));
